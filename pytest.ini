[pytest]
# Only collect the real suite. The root-level test_brand.py / test_client.py /
# test_product.py are hand-run diagnostic probes that hit the live site on
# import — they must never be picked up by a CI run.
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
