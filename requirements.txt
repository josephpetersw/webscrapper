aiofiles==25.1.0
beautifulsoup4==4.15.0
blinker==1.9.0
certifi==2026.7.22
cffi==2.1.0
click==8.4.2
curl_cffi==0.15.0
et_xmlfile==2.0.0
Flask==3.1.3
flask-cors==6.0.5
# Unix-only: gunicorn has no native Windows support (the README's Option B,
# `python app.py`, is the Windows path). Without this marker `pip install -r`
# fails outright on Windows.
gunicorn==26.0.0; sys_platform != "win32"
itsdangerous==2.2.0
Jinja2==3.1.6
lxml==6.1.1
markdown-it-py==4.2.0
markdownify==1.2.3
MarkupSafe==3.0.3
mdurl==0.1.2
numpy==2.5.1
openpyxl==3.1.5
packaging==26.2
pandas==3.0.5
pycparser==3.0
Pygments==2.20.0
python-dateutil==2.9.0.post0
rich==15.0.0
six==1.17.0
soupsieve==2.9.1
typing_extensions==4.16.0
# Unix-only, and nothing imports it — but pip still tries to *build* it on
# Windows and fails, which took the whole install down with it.
uvloop==0.22.1; sys_platform != "win32"
Werkzeug==3.1.8

# ── Optional ─────────────────────────────────────────────────────────────
# undetected-chromedriver adds a real-browser rung to the fetch ladder, tried
# only after every curl_cffi fingerprint has been refused (see
# scraper/browser_client.py). Deliberately NOT installed by default: it pulls
# in Selenium and a full Chrome, and the overwhelming majority of stores never
# need it. Install it only if you are scraping a site behind an interactive
# JavaScript challenge:
#
#     pip install undetected-chromedriver
#
# Without it, HAS_BROWSER is False and the rung is simply absent.
